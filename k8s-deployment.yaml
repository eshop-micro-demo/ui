apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui
  labels:
    app: microshop
    component: ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: microshop
      component: ui
  template:
    metadata:
      labels:
        app: microshop
        component: ui
    spec:
      containers:
      - name: ui
        image: quay.io/microshop/ui:latest
        ports:
        - containerPort: 3000
        env:
        - name: STORE_URL
          value: "http://store.dh4r4pvj.ga"
        - name: CHECKOUT_URL
          value: "http://checkout.dh4r4pvj.ga"
---
apiVersion: v1
kind: Service
metadata:
  name: ui
  labels:
    app: microshop
    component: ui
spec:
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: microshop
    component: ui
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ui
  annotations:
spec:
  rules:
    - host: dh4r4pvj.ga
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ui
                port:
                  number: 80